# Development conda environment
# usage: $ mamba env create -f environment.yml
name: somata
channels:
  - conda-forge
dependencies:
  - numpy==1.23
  - python>=3.8
  - codetiming
  - colorcet
  - ipdb
  - joblib
  - kneed
  - matplotlib
  - pip
  - pytest
  - scipy
  - setuptools
  - spectrum
  - tqdm
